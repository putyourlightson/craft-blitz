{% extends 'blitz/_utilities/diagnostics/layouts/base' %}

{% set crumbs = [
    {
        label: 'Tracked Query String Params',
        path: 'query-string-params',
    },
    {
        label: param,
        path: (isLeaf ?? false) ? null : 'query-string-params/param?param=' ~ param,
        class: (isLeaf ?? false) ? 'leaf code' : 'code',
    },
]|merge(crumbs ?? []) %}

